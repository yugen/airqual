# Functional Evidence Interface

## Installation & Deployment
Requirements:
* Docker
* Docker Compose
* node/npm 14 (TODO: build js assets w/ DOCKER)

```
$ cd frontend
$ npm install
$ npm run build
$ cd ../
$ docker-compose up --build -d
```
